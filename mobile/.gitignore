node_modules/

# Expo generated native directories (managed workflow - regenerated by prebuild).
# Anchored: unanchored patterns also swallow plugins/android and plugins/ios,
# which hold the native sources the config plugins copy in.
/android/
/ios/

# Expo cache
.expo/

# Build output
dist/

# Environment
.env
.env.*
!.env.example
